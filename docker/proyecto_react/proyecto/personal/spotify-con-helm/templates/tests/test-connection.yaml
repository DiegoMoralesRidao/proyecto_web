apiVersion: v1
kind: Pod
metadata:
  name: '{{ include "spotify-con-helm.fullname" . }}-test-connection'
  labels:
    {{- include "spotify-con-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spotify-con-helm.fullname" . }}:{{ .Values.app.service.port }}']
  restartPolicy: Never

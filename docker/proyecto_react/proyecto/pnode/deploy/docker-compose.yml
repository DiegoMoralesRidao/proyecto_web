services:
  web:
    build:
      context: ${CONTEXTO}
      dockerfile: ./dockerfiles/node/dockerfile
    container_name: ${NOMBRE}
    ports:
      - "${PORT_WEB}:80"    
    env_file:
      - ./.env
    image: diego/nodejs
    volumes:
      - ./logs:/root/logs      
    networks:
      netproyecto_web:
        ipv4_address: ${IP}
networks:
  netproyecto_web:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: ${SUBNET}
services:
  web:
    build:
      context: /home/diego/proyecto_web/docker/proyecto_react/
      dockerfile: ./dockerfiles/ubsecurity/dockerfile
    container_name: ctspotify
    image: dmorrid/ubsecurity
    ports:
      - ${PORT_SSH}:80
    env_file:
      - ./.env
    volumes:
      - ./logs:/root/logs 
    networks:
      netproyecto_web:
        ipv4_address: ${IP_WEB}
networks:
  netproyecto_web:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: ${SUBNET}